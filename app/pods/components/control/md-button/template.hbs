{{#if (has-block)}}
  {{yield}}
{{else}}
  {{#if icon}}
    {{fa-icon icon spin=iconSpin}}
  {{/if}}
  <span class={{if required "required"}}>{{text}}</span>
{{/if}}

{{#if tooltip}}
  {{ember-tooltip tooltipClass=(concat "ember-tooltip md-tooltip " tipClass) text=tooltip side=tipSide}}
{{/if}}
